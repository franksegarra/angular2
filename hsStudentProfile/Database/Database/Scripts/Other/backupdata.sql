/*
select * into activity from hsplayerprofile..activity
select * into activityType from hsplayerprofile..activityType
select * into highSchool from hsplayerprofile..highSchool
select * into siteFeedback from hsplayerprofile..siteFeedback
select * into student from hsplayerprofile..student
select * into studentActivities from hsplayerprofile..studentActivities
select * into studentBaseball from hsplayerprofile..studentBaseball
select * into studentBBHitting from hsplayerprofile..studentBBHitting
select * into studentClasses from hsplayerprofile..studentClasses
select * into studentCoaches from hsplayerprofile..studentCoaches
select * into studentContact from hsplayerprofile..studentContact
select * into studentExtraCurricular from hsplayerprofile..studentExtraCurricular
select * into studentLinks from hsplayerprofile..studentLinks
select * into studentPictures from hsplayerprofile..studentPictures
select * into studentPositions from hsplayerprofile..studentPositions
select * into studentSchedules from hsplayerprofile..studentSchedules
select * into studentVideos from hsplayerprofile..studentVideos
select * into userLoginHistory from hsplayerprofile..userLoginHistory
select * into userRoles from hsplayerprofile..userRoles
select * into users from hsplayerprofile..users
*/